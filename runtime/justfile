default:
    @just --list

# Restore NuGet packages
init:
    dotnet restore

# Build the runtime
build:
    dotnet build

# Run tests
test:
    dotnet test --verbosity quiet

# Format code and apply lint fixes
fix:
    dotnet format

# Check formatting without changes
check:
    dotnet format --verify-no-changes

# Publish NativeAOT shared library
publish:
    dotnet publish Gaffer.Runtime -c Release -r linux-x64

# Remove build artifacts
clean:
    rm -rf Gaffer.Runtime/bin Gaffer.Runtime/obj Gaffer.Runtime.Tests/bin Gaffer.Runtime.Tests/obj Gaffer.Sdk/bin Gaffer.Sdk/obj
