version: "2"

linters:
  enable:
    - revive
    - forbidigo
    - errorlint
    - intrange
    - modernize
    - bodyclose
    - perfsprint
    - usetesting
    - unconvert
    - copyloopvar
    - misspell

  settings:
    revive:
      rules:
        - name: blank-imports
        - name: context-keys-type
        - name: error-return
        - name: range
        - name: time-naming
        - name: errorf
        - name: empty-block
        - name: unreachable-code
        - name: unexported-return
        - name: use-any

    forbidigo:
      forbid:
        - pattern: '^sort\.(Strings|Ints|Float64s|Slice|SliceStable)$'
          msg: Use slices.Sort / slices.SortFunc (go 1.21+), not sort.*

    usetesting:
      # Several tests unset an env var for their duration and restore it in a
      # Cleanup. t.Setenv can't express "unset", so os.Setenv stays here.
      os-setenv: false

  exclusions:
    generated: lax
    presets:
      - comments
      - common-false-positives
      - legacy
      - std-error-handling

formatters:
  enable:
    - goimports
    - gofumpt
  settings:
    golines:
      max-len: 120
