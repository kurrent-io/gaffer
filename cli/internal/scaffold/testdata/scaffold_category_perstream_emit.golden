fromCategory('order')
  .foreachStream()
  .when({
    $init: function() {
      return {};
    },
    // Add your event handlers here
    // EventType: function(state, event) {
    //   emit('stream-name', 'EmittedType', { data: event.data });
    //   return state;
    // }
  })
