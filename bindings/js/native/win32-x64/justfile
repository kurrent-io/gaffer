default:
    @just --list

# Copy the runtime .dll into this package (requires runtime publish for win-x64)
build:
    cp ../../../../runtime/Gaffer.Runtime/bin/Release/net10.0/win-x64/publish/Gaffer.Runtime.dll gaffer.dll

# Remove build artifacts
clean:
    rm -f gaffer.dll
