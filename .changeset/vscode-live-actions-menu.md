---
"gaffer-vscode": patch
---

The per-projection **Manage...** menu now updates live while an environment's status is still loading. It opens immediately, shows a **Loading status…** row for any environment still being read, and fills in that environment's actions in place as the status resolves - so a still-loading environment is no longer a dead-end that needed closing and reopening.
