default:
    @just --list

# Build TypeScript
build:
    pnpm run build

# Run tests
test:
    pnpm run test

# Type check, lint, and format check
check:
    pnpm run check

# Format code and apply lint fixes
fix:
    pnpm run fix

# Remove build artifacts
clean:
    rm -rf dist
