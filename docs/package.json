{
  "name": "@kurrent/gaffer-docs",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "scripts": {
    "sync-telemetry": "node scripts/sync-telemetry-notices.mjs",
    "sync-diagnostics": "node scripts/sync-diagnostics.mjs",
    "sync": "pnpm run sync-telemetry && pnpm run sync-diagnostics",
    "dev": "pnpm run sync && astro dev",
    "start": "pnpm run sync && astro dev",
    "build": "pnpm run sync && astro build",
    "preview": "astro preview",
    "check": "pnpm run sync && astro check",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.4",
    "@astrojs/cloudflare": "^13.5.3",
    "@astrojs/starlight": "^0.39.2",
    "astro": "^6.3.6",
    "astro-rehype-relative-markdown-links": "^0.19.0",
    "sharp": "^0.34.5",
    "typescript": "^6.0.3"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "wrangler": "^4.93.0"
  }
}
