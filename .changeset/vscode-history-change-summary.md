---
"gaffer-vscode": patch
---

The history viewer now shows what a metadata-less update changed, e.g. `query changed`, matching the terminal `gaffer history`. An out-of-band edit reads `query changed outside gaffer` rather than the less specific `changed outside gaffer`.
