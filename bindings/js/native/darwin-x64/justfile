default:
    @just --list

# Copy the runtime .dylib into this package (requires runtime publish for osx-x64)
build:
    cp ../../../../runtime/Gaffer.Runtime/bin/Release/net10.0/osx-x64/publish/Gaffer.Runtime.dylib gaffer.dylib

# Remove build artifacts
clean:
    rm -f gaffer.dylib
