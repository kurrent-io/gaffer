---
"@kurrent/gaffer": patch
---

The `gaffer init` template's comment now says at most one environment may be the default; a config with none is valid (`--env` or the interactive picker selects instead).
