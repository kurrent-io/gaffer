# Codegen outputs - regenerated by `just telemetry build` from schemas/.
generated/*
# go.mod is committed: declares the module path for the generated Go
# package, which the workspace (go.work) wires up. The .go file alongside
# is regenerated.
!generated/go.mod
