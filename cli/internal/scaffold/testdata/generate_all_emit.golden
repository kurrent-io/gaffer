fromAll()
  .when({
    $init() {
      return {};
    },
    // Add your event handlers here
    // EventType(state, event) {
    //   emit('stream-name', 'EmittedType', { data: event.data });
    //   return state;
    // }
  })
