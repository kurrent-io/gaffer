---
"gaffer-vscode": patch
---

The extension's marketplace title is now "KurrentDB Gaffer (Projections tooling)" and all in-editor surfaces use the short brand "Gaffer". Command-palette entries read `Gaffer: Debug`, `Gaffer: Scaffold`, and so on; the output channels, panel, and notifications say "Gaffer" instead of "KurrentDB Projections".
